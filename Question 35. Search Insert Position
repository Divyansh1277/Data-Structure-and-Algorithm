class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        int idx = 0;
        if(target>nums[nums.size()-1]) return nums.size();
         for(int i=0;i<nums.size();i++){
            if(target<=nums[i]){ idx = i; break;}
         }
         return idx;
    }
};
